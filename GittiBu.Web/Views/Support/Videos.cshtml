@model string

@{
    var lang = Constants.GetLang(Context.Session.GetString("lang"));
    ViewBag.Title = Localization.Get("Videolar", "Videos", lang);
    Layout = "_Layout";
}

<div class="section-1 sec-altsayfa sec-hesaplar">
    <div class="w-container">

        @{
            await Html.RenderPartialAsync("SupportSideMenu", 1);
        }

        <div class="content w-clearfix">
            <div class="div-block-20 div-ilan hesap w-clearfix">
                <h4 class="h2 h4 h-kat h-kategori">@Localization.Get("Videolar", "Videos", lang)</h4>
            </div>
            <div class="product product-list urulerim bannerlar">

                @Html.Raw(Model)

            </div>
            <div class="product product-list urulerim bannerlar"></div>
        </div>

    </div>
</div>
